{
  "name": "vocab-hero",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:vercel": "vite build && npx vercel deploy --prod ./dist",
    "deploy:surge": "vite build && npx surge ./dist vocab-hero-app.surge.sh",
    "deploy:netlify": "vite build && npx netlify deploy --dir=./dist --prod"
  },
  "dependencies": {
    "@google/genai": "^1.41.0",
    "@vercel/postgres": "^0.10.0",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vercel/node": "^5.6.3",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
